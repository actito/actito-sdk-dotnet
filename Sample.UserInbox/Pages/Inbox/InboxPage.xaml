<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Sample.UserInbox.Models"
             xmlns:inboxViews="clr-namespace:Sample.UserInbox.Pages.Inbox.Views"
             x:Class="Sample.UserInbox.Pages.Inbox.InboxPage"
             x:Name="This"
             Title="Inbox">

    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding RefreshCommand}">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="MaterialIconsRegular"
                    Color="Black"
                    Size="Title"
                    Glyph="{x:Static models:Icons.Refresh}" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Items}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <inboxViews:InboxItemView
                        Item="{Binding .}">

                        <inboxViews:InboxItemView.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={x:Reference This}, Path=ItemTappedCommand}"
                                CommandParameter="{Binding}" />
                        </inboxViews:InboxItemView.GestureRecognizers>
                    </inboxViews:InboxItemView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>